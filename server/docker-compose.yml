version: '3.1'

services:
  db:
    container_name: postgres_reiventar
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: root
      POSTGRES_DB: reiventar
    ports:
      - "5432:5432"